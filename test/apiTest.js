const { equal } = require('assert');


describe("Testing the lingo bingo app api endpoints", () => {
    beforeEach(async () => {
        // await pool.query('DELETE FROM waste_donor');
        // await pool.query('DELETE FROM waste_bin');
    });

    it('Should return pass the test', async () => {
        equal(0,0);
    });

    after(() => {
        // pool.end();
    });

});